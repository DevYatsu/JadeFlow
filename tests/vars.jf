const x: num = 5788887, mut z: num =  2 + 9.3 // hey test line comment
const test = (100 * 0) + 2 - (2*1)

z += 7
const string: str = "test"
const arr2: vec=[2,4**2,"test", 9.4, [3], x]

const fstr: str = `hey dude #{string}`

const nonono: num = null
const bool: bool = true
const bool2=false

test and dude
hey or dude

if z?>x {
    x;
} else if z?=x {
    z--
}

match bool {
    true {
        // do something
    }
    false? print("false"),
}

/*
const str = str("haha")
const str2 = new str("hehe")
const obj = dict({})
const obj2 = {}

const arr = array([2,3,4])

const x = 5
const z = 9.1

const bool = true
cosnt bool2 = false

str = str("test");
str2 = "test";

const arr = array([2,3,4]);
const arr2 = [];

const obj = dict({});
const obj2= {};
*/

mut hey=23+40 
hey=2+2


fn hey(dudem, difjd, dfdf) {
    return dudem
}
fn test() {
    >>"hello"
}
class Human {}


fn main() {
    const num = 10
    for i in 0->num { // range non including 10
        print(fibonacci(i))
    }
    for i in 0=>num { // range including 10
        print(fibonacci(i))
    }
}