mut 
    test1 = 23 + 2, 
    t: bool = true, 
    test1 = "str", 
    test2 = ["test string", 0, false, [4], {t}], 
    test3 = {
        test2,
        test4: t,
        test1
    }

const hey = 1 + 3

const all = {
    test1,
    t,
    "efef": test1,
    test2,
    test3: test3 + 1
}

const test_arr = [
    test1 + 2,
    t,
    test2,
    test3
]

const test = "string for test" 
const formatted = `hey dude #{test}`
const calc3 = `3 = #{1 + 2}`

const a = {o: [2]}
const arr = ["test", "str", 3, a, [2]]

const b = arr[0]
const arr = arr + arr[2] * 2


const hey = concat("str", " test")

fn concat(s: str, s1: str): str => s + s1