mut 
    test1 = 23 + 2, 
    t: bool = true, 
    test1 = "str", 
    test2 = ["test string", 0, false, [4], {t}], 
    test3 = {
        test2,
        test4: t,
        test1
    }

const 
    hey = 1 + 3,
    all = {
        test1,
        t,
        "efef": test1,
        test2,
        test3: test3 + 1
    },
    test_arr = [
        test1 + 2,
        t,
        test2,
        test3
    ]

const test = "string for test" 
const formatted = `hey dude #{test}`
const calc3 = `3 = #{1 + 2}`

const a = {o: [2]}
const arr = ["test", 1, 3, a, [2]]

const y = 4
const x = y
const b = arr[x][0]
const arr = arr 
+ arr[x-2] 
* 2 /
b


const hey = concat("str", " test")

fn concat(s: str, s1: str): str => s + s1