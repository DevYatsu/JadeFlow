class Human (name: str, age: num, gender: str) {
    ctx.name = name
    ctx.age = age
    ctx.gender = gender
    // can be accessed anywhere in the class and outside of it

/*
    pub {
        ctx.counter = 0
        // can be accessed in this block and outside

        fn presentation(ctx) {
            print("I am #{ctx.age} years old #{ctx.gender}, my name is #{ctx.name}!")
        }

        fn increase_age(ctx) {
            ctx.age += 1
        }
    }

    priv {
        ctx.hey = `hey #{ctx.name}`
        // can only be accessed here
    }*/
}

/*const human = Human('Bob', 23, 'male')
human.presentation()
human.increase_age()*/