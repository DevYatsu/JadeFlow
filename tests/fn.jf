const x = "ssss"

fn obj_from (s: str, s2: str,): dict => {s, s2}
fn hello() {
    print("Hello, world!")
}

hello()

fn duplicate(mut hey: str): str {
    hey = hey + hey
    >> hey
}
println(duplicate("dude"))