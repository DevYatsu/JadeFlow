fn concatenate(s: str, s2: str): str {
    mut o = s + s2

    return o
}
// fn concatenate (s:str, s2:str): str => s + s2

fn obj_from (s: str, s2: str,): dict => {s, s2}

fn concatenate_several(s: str, s2: str, s3: str, s4: str): str {
    >> concatenate ( concatenate(s, s2), concatenate s3, s4) )
}